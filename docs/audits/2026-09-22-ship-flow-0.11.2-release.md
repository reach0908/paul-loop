# ship-flow 0.11.2 배포 준비

사용자 승인: “배포도 알아서 진행하고 개선점들도 계속해서 개발 진행해줘”.
대상은 `reach0908/paul-loop`의 provider PR → main → 버전 태그/생성 artifact 경로다.
소비자 설치·설정·메모리 인프라 변경은 포함하지 않는다.

## 변경과 기준

- base: `30cafaaec6ae53d0e18e0dcf883c30700be0c702` (`origin/main`).
- 앞서 검토한 `fcbfd32`, `79b76e1`, `112e5b5`의 provider 구현을 선별 반영했다.
  기존 연구·소비자 진단 문서는 원래 연구 worktree에 보존했다.
- main에 배포된 triage/prototype backport 및 vendor 이력은 유지한다.
- 질문·작은 수정·일반 Git 작업의 full ship/publisher 자동 진입을 줄이고, 다음 작업도 원래 목표·저장소 범위를 유지한다.
- Codex 변환에서 수동 스킬과 publisher의 암묵 호출을 제한한다. 새 metadata 지원 범위는
  `docs/runtime-compatibility.md`에 명시했다. 더 복잡한 YAML은 조용히 정책을 잃지 않고 오류로 반환한다.
- TDD/review의 최소 구현·재사용 원칙, 반복 지침 정리와 실패 후 검증 재시작 지침을 포함한다.
  필수 검증·리뷰·독립 발행·승인 경계는 유지한다.

## 검증과 리뷰

- 통합 후 installer + runtime package: 98 PASS / 0 FAIL / 1 SKIP.
  실제 Codex 설치 probe는 opt-in으로 미실행했다.
- Standards 독립 리뷰: actionable finding 없음.
- Spec 독립 리뷰: YAML 주석/quoted key에서 수동 호출 정책을 잃는 P2 발견.
  literal boolean·quoted key·line comment 처리와 alias/duplicate 거부, native metadata shape 거부로 수정했다.
  회귀 2개 PASS, 수정 후 독립 Spec 재검토 및 generator 재현 PASS.
- vendor lock 24개, lock consistency, runtime 생성·재생성 비교, source strict manifest PASS.
- 첫 classifier 실행은 기본 base `origin/develop`을 찾지 못해 exit 2였다.
  저장소의 실제 base `origin/main`을 명시하여 재실행했다. 범위가 넓어 REQUIRE이며 위 배포 위임을 사용한다.
- 전체 engine·memory·runtime·보안 검사 및 배포는 PR/main의 해당 커밋 CI 결과를 확인한 뒤 기록한다.

실제 모델의 선택 정확도·속도·장기 메모리 효용은 측정하지 않았다.
생성 artifact·단위검사·CI 통과는 소비자 활성화의 증거가 아니다.
